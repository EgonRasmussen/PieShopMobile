<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BethanysPieShopMobile.EntryPage"
             BackgroundImage="patternwide.png">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>

                <Label Text="Working with the Entry" FontSize="Large" />

                <Entry Text="Enter your user name" />
                <Entry Text="User name" MaxLength="15" />
                <Entry Placeholder="User name" />
                <Entry  IsPassword="True" />

                <!--Keyboard-->
                <Entry Text="Phone number" Keyboard="Telephone" />
                <Entry Text="URL" Keyboard="Url" />
                <Entry Placeholder="Enter text here">
                    <Entry.Keyboard>
                        <Keyboard x:FactoryMethod="Create">
                            <x:Arguments>
                                <KeyboardFlags>Suggestions,CapitalizeWord</KeyboardFlags>
                            </x:Arguments>
                        </Keyboard>
                    </Entry.Keyboard>
                </Entry>
                <Entry Text="Special return" ReturnType="Send" />

                <!--UI properties-->
                <Entry Text="Enter some text here" BackgroundColor="Bisque" TextColor="Aquamarine" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>