<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BethanysPieShopMobile.ViewBindingPage">

    <StackLayout BindingContext="{x:Reference Name=PriceSlider}"
                Padding="20"
                Spacing="20">

        <Slider x:Name="PriceSlider" 
        MinimumTrackColor="Green"
        MaximumTrackColor="Red"            
        Minimum="0" 
        Maximum="100" />

        <Label Text="{Binding Source={x:Reference Name=PriceSlider}, Path=Value}" />

        <Label Text="{Binding Path=Value}" />

        <Entry Text="{Binding Value}" />

        <!--<Label Text="{Binding Value, StringFormat='Rotation angle = {0:N}'}" 
               Rotation="{Binding Value}" 
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand"
               FontSize="Large" />-->

    </StackLayout>

</ContentPage>